import React from "react";
// import style from './index.less';

const QueryContainer = (props: any) => {
  return <div className="hm-query-container">{props.children}</div>;
};

export default QueryContainer;
